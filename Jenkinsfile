pipeline {
        agent any
        stages {
          stage('stage`')
            steps (sh 'touch jenkins.txt')
        }

        stage('stage2') {
          steps {sh 'touch jenkins2.txt'}
        }

        stage('stage') {
          steps {sh 'touch jenkins3.txt'}
        }

      }  
}
